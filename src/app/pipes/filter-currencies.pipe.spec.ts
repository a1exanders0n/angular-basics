import { FilterCurrenciesPipe } from './filter-currencies.pipe';

describe('FilterCurrenciesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCurrenciesPipe();
    expect(pipe).toBeTruthy();
  });
});
